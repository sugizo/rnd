language: none

build:
  cache: true
  pre_ci_boot:
    image_name: ubuntu
    image_tag: latest
  ci:
    - shippable_retry apt update
    - shippable_retry apt install -y npm nodejs-legacy
    - shippable_retry npm list harp || npm install harp -g
    - harp compile
